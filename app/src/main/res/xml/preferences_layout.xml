<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen
    xmlns:android="http://schemas.android.com/apk/res/android">

    <Preference
        android:title="Home Location"
        android:summary="Set your Home as a trip starting point">
        <intent android:action="ericrybarczyk.me.roadtrippy.HomeLocationPreference" />
    </Preference>

    <EditTextPreference
        android:key="@string/pref_key_daily_driving_hours"
        android:title="Daily Driving Duration"
        android:defaultValue="12"
        android:inputType="number"
        android:numeric="integer"
        android:digits="0123456789"
        android:summary="Drives over this limit will be split to separate days"
        android:dialogTitle="Maximum Driving Hours per Day" />

</PreferenceScreen>